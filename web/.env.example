# Base URL of the CLARIS FastAPI backend. Every fetch and the WebSocket derive from this.
# Leave unset for local dev (defaults to http://localhost:8000). On a static Vercel deploy
# with no backend, the app detects the unreachable socket and switches to labeled demo mode.
VITE_API_URL=http://localhost:8000
